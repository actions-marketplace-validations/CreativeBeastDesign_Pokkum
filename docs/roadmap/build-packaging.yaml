area: "Build & Packaging"
items:
  # ---------------------------------------------------------------------
  # Shipped — core compilation & packaging (Feature-list.md "Core
  # Compilation & Container Packaging")
  # ---------------------------------------------------------------------

  - id: vendor-production-dependencies
    title: "Ship production dependencies so images are self-contained"
    status: shipped
    kind: fix
    tier: foundation
    summary: "Layered images shipped the server bundle without its dependencies; Bun's runtime auto-install silently fetched them from npm inside the running container."
    problem: |
      Vite externalises dependencies during the SSR build, so adapter-node output keeps bare
      imports and expects a node_modules tree beside it — its documented deployment model is
      "ship build/ AND production node_modules". Pokkum shipped only the first half.

      On Bun the gap was masked rather than visible: the runtime auto-install fetched missing
      packages from the public npm registry into the running container, so the app appeared to
      work while executing code that is in neither the image, the SBOM, the signature nor the
      provenance. Under `pokkum resolve`'s own `readOnlyRootFilesystem` the write failed and
      the pod crash-looped after reporting a successful rollout; and because the default
      NetworkPolicy permits TCP 443, the fetch succeeds in-cluster until an operator tightens
      egress, at which point a working deployment breaks with no change to the image. On Node,
      which has no auto-install, any route touching an externalised dependency simply 500s.
    decision: |
      Shipped 2026-08-21, found by an adversarial field test against a real application.

      `Prepare` installs the project's production dependencies into a staging tree under
      `.pokkum/` (never touching the user's own tree) and the packager mounts it at
      **/app/node_modules**. The path is load-bearing: Node and Bun resolve a bare import by
      walking upward from the importing file, so `/app/server/index.js` finds
      `/app/node_modules` and never `/app/vendor` — which is where the pre-existing vendor
      layer pointed, and which is why populating that layer would not have worked. That layer
      turned out to be dead wiring: the pipeline pointed it at `<output>/vendor` and nothing
      ever created the directory.

      A missing lockfile warns rather than refusing. Making it an error was the first cut and
      was over-reach: it turned a reproducibility preference into a gate on building at all
      and refused every project without a lockfile, including three of the repo's own
      fixtures. With a lockfile the install uses `--frozen-lockfile`, so versions are exactly
      what is pinned; hermetic builds add `--offline` and fail with a warm-the-cache hint
      rather than silently shipping an image without its dependencies.

      The image entrypoint now also passes Bun's `--no-install`, so a remaining gap fails
      loudly at startup instead of being papered over by a network fetch. Verified in both
      directions: an `await import("valibot")` in an empty project resolves by downloading the
      package at runtime, and the same import under `--no-install` fails with "Cannot find
      package".
    limitations:
      - "Build-time detection of a dependency that will be missing at runtime shipped separately on 2026-08-22 — see [Detect dependencies that will be missing at runtime](item:unresolved-import-guard). An earlier static-analysis attempt was reverted for failing correctly-configured builds; the shipped guard reads the manifest adapter-node externalises from instead of scanning the bundle."
      - "`--runtime=node` images benefit automatically (they previously had no dependency store at all), but that combination has not been re-tested end to end against a real application since the fix."
    impl:
      - internal/adapters/bunexec/vendor_install.go
      - internal/adapters/packager/packager.go
      - internal/ports/packager.go

  - id: version-name-pin
    title: "Pin kit.version.name so correctly-configured projects build reproducibly"
    status: shipped
    kind: fix
    tier: foundation
    summary: "The reproducibility pin lived inside the adapter-injection path, so it only ran for misconfigured projects — a correctly configured one got no pin and a different image on every build."
    problem: |
      SvelteKit defaults `kit.version.name` to `Date.now()`. That value lands in
      `_app/version.json` and participates in client chunk naming, so two builds of an
      unchanged tree produce different filenames and different layer digests — the exact
      property Pokkum sells.

      Pokkum did pin it, but only as a side effect of staging a Vite config while injecting a
      corrected adapter. A project whose adapter was already right never entered that path,
      so it got no injection, no pin, and non-reproducible images. The behaviour was
      inverted: misconfiguring your project was the only way to get a reproducible build.

      Measured on a real component library: two consecutive builds of an unchanged tree
      emitted `{"version":"1787392160265"}` and `{"version":"1787392373500"}`, renaming every
      hashed chunk and differing two OCI layers.
    decision: |
      Shipped 2026-08-22. The pin now runs as its own guard, independent of whether an adapter
      injection happened, and covers both outcomes explicitly:

      Where the project's build script is exactly `vite build`, Pokkum stages a Vite config
      solely to pin the version and runs that. Where it does more (`vite build && bun run
      prepack`), taking the build over would silently skip the rest, so Pokkum leaves it alone
      and warns that the image is not bit-for-bit reproducible, naming the one-line fix
      (`kit.version.name = process.env.SOURCE_DATE_EPOCH`, which Pokkum exports into the build
      environment).

      Verified end to end against a real project: pinning the version name made all 288
      emitted client filenames identical between builds, and the warning fires on the real
      multi-command build script that cannot be taken over.
    limitations:
      - "The staged-config path is covered by unit tests driving `Prepare`, but has not been exercised end to end against a real single-command project — the project it was verified against is on the warn path."
      - "Pinning the version name is necessary but not sufficient: prerendered HTML containing non-deterministic component IDs (`Math.random()` rather than Svelte's `$props.id()`) still differs between builds. That is a property of the user's source, not of Pokkum."
    impl:
      - internal/adapters/bunexec/compiler.go
      - internal/adapters/sveltekitutils/injector.go

  - id: unresolved-import-guard
    title: "Detect dependencies that will be missing at runtime"
    status: shipped
    kind: hardening
    tier: polish
    summary: "A build-time check that every externalised dependency resolves inside the image, read from the manifest adapter-node itself externalises from rather than from the bundle."
    problem: |
      [Ship production dependencies so images are self-contained](item:vendor-production-dependencies)
      fixes the images built today. Nothing yet catches the next time something falls out of
      the tree, and the failure it would cause is invisible until a request arrives in
      production: the container starts, both probes pass, and the first route touching the
      missing package 500s.

      A static-analysis version was written and **reverted**, because it failed real,
      correctly-configured builds. Recording why, so it is not attempted the same way twice:

      - **Type-only imports survive as text.** The adapter-node fixture's built server
        contains `from 'types'` eleven times — a type import erased at runtime, naming a
        package that does not exist. A regex over the bundle cannot tell it from a real one.
      - **adapter-node bundles most dependencies.** The fixture declares zero production
        dependencies, its built output still contains bare imports for `svelte`,
        `@sveltejs/kit`, `polka`, `sirv` and `cookie`, and its image boots and serves traffic
        (`TestRuntimeSmoke_LayeredStrategy_BootsAndServes`). The remaining specifiers are
        artifacts of bundling, not runtime requirements.

      So "appears as a bare import in the output" does not imply "must resolve at runtime",
      and a guard that fails a working build is worse than the bug it guards.
    decision: |
      Shipped 2026-08-22, taking the first option but arriving at it from the other end.

      Vite does not expose its externals list: `shouldExternalize` caches verdicts in a
      closure-local map and a module-local WeakMap, neither exported (vite 8.2.1), and the
      manifest SvelteKit forces (`manifest: true`) contains only internal chunk keys. Neither
      adapter-node version installed here writes a metafile.

      It turns out not to be needed. adapter-node's rollup `external` option is literally

          ...Object.keys(pkg.dependencies || {}).map(d => new RegExp(`^${d}(\/.*)?$`))

      (5.5.7 `index.js:76-79`; 6.0.0-next.10 adds `@opentelemetry/api`). Everything else,
      every devDependency included, is bundled into the output. So the set of specifiers that
      must resolve at runtime *is* `package.json`'s `dependencies` — read from the same file
      the bundler read it from. The guard checks each one resolves as a package directory
      (with its own `package.json`, since that is what Node resolves through) in the staged
      tree, and fails the build naming the missing packages.

      Both failure modes that got the previous attempt reverted are unreachable by
      construction rather than worked around: a bundled-away package is by definition absent
      from `dependencies`, and a specifier appearing only inside a JSDoc comment
      (`/** @import { X } from 'types' */` — which is what the reverted regex was actually
      tripping on) never enters a manifest.

      Verified on a real layered build of `testdata/fixtures/sveltekit-adapter-node`: `clsx`
      is declared in `dependencies`, adapter-node left it external (`from 'clsx'` in the built
      output), it was staged, and it shipped to `app/node_modules/clsx` — the guard passed
      without a false positive. The failure direction is covered by a Prepare-level test whose
      fake `bun install` exits 0 having staged nothing, which is exactly how the real command
      behaves when the lockfile disagrees with the manifest.

      Fixture correction found on the way: the in-test `validPackageJSON` declared
      `@sveltejs/kit` under `dependencies`, where no real scaffold puts it. That described a
      project that could not work — kit would have been externalised and then absent — so the
      guard was right and the fixture was wrong.
    limitations:
      - "Scoped to the layered strategy, which is the only one with a runtime dependency tree. `exe` compiles a self-contained binary and `static` ships no JS runtime."
      - "adapter-node 6 externalises `@opentelemetry/api` unconditionally, beyond `dependencies`. A project using it without declaring it would not be caught."
      - "A production dependency that is declared but never imported still has to be installed to pass. That is a manifest that overstates its needs rather than a false positive, and the fix — move it to devDependencies — is in the error message."
    options:
      - title: "Read the bundler's own externals list"
        description: "Vite/Rollup knows exactly which modules it externalised. Capture that during the build (a metafile, or an explicit ssr.external configuration Pokkum sets) and check only those specifiers against the staged tree."
        tradeoffs: "Exact rather than heuristic, and cheap at build time — but requires reaching into the build's own output format, which differs across Vite versions and is not a stable contract."
      - title: "Verify empirically at build time"
        description: "After building, start the produced server with Bun's --no-install in the staged environment and confirm it reaches a listening state."
        tradeoffs: "Authoritative — it is what actually happens in production — but slow, needs a free port, and turns every build into a partial smoke test."
      - title: "Rely on the runtime check already in place"
        description: "The image entrypoint passes --no-install, so a missing dependency now fails loudly at startup instead of being silently fetched from npm."
        tradeoffs: "Zero cost and already shipped, but the failure surfaces at deploy time rather than build time — better than production, worse than CI."
    recommendation: |
      First option. The bundler is the only component that actually knows which specifiers it
      externalised, and every heuristic that does not consult it will keep mistaking type
      imports and bundled-away packages for real dependencies.

      Until then the third option is genuinely load-bearing rather than a placeholder: with
      `--no-install` in the entrypoint, a gap fails at container start with "Cannot find
      package", which is loud, immediate and honest — the property that was missing when Bun
      silently downloaded the difference from npm.
    related: ["route-exclusion-filter"]
    impl:
      - internal/adapters/bunexec/unresolved_imports.go
      - internal/adapters/bunexec/compiler.go

  - id: route-exclusion-filter
    title: "Exclude routes from the production build"
    status: shipped
    kind: feature
    tier: moat
    summary: "Dev-only routes are bundle entry points, so tree-shaking cannot remove them; a build-time filter would keep their code out of the image entirely — which output filtering cannot do."
    problem: |
      A project with dev-only routes — `/dev/style-guide`, a component gallery, a fixtures
      browser — ships them to production. They are `+page.svelte` files, which makes them
      bundle **entry points**, and no amount of tree-shaking removes an entry point:
      reachability is its definition. Everything they import ships with them, and a style
      guide typically imports the entire component library on purpose, so it can be one of
      the largest things in the image.

      SvelteKit offers no way to exclude them. Checked against kit 2.70.2's
      `src/core/config/options.js`: there is `moduleExtensions` and `files.*`, and nothing
      that drops routes. The community answer is a runtime guard — a `+layout.server.ts`
      that throws 404 unless `dev` — which controls *access* while leaving the code, its
      imports, and its dependencies in the image and in the SBOM.

      Note what is NOT a problem, since it is the common worry: colocated test files are
      already safe. SvelteKit's routes walker skips any file not starting with `+`
      (`create_manifest_data/index.js:237`), so `foo.test.ts` beside a `+page.svelte` is
      invisible to the router. It only ships if route code imports it.
    decision: |
      Shipped 2026-08-22, taking the first option after proving it against real builds rather
      than reasoning about it. Justified on developer experience rather than size: the
      measurement below came out modest, and the reason to have it is being able to keep as
      many `/dev/` routes in a project as you like knowing none of them ship.

      `kit.files.routes` is pointed at `.pokkum/routes`, a mirror of the routes directory
      built from symlinks with the excluded routes left out. Symlinks rather than copies are
      required for correctness: Vite resolves a symlinked module to its real path, so a
      route's relative import that escapes the routes tree (`../../lib/thing.js`) still
      resolves. A copied tree breaks every one of those.

      There is no supported way to point SvelteKit at a different config file — no
      `SVELTE_CONFIG_PATH`, no CLI flag, and Kit sets vite-plugin-svelte's `configFile: false`
      unconditionally. The supported override is passing config inline to the `sveltekit()`
      plugin, which Pokkum already does for adapter injection and the version pin; routes are
      one more injected key, applied as a wrapper so the object is evaluated once and any
      `files` the project set survives.

      Three guards, each from an observed failure rather than a precaution:

      - `resolve.preserveSymlinks: true` is refused. It makes every escaping relative import
        fail to resolve — reproduced as a real `UNRESOLVED_IMPORT` build failure.
      - A partially excluded directory is recreated with **all** its surviving entries,
        layouts included. A mirror that dropped `admin/+layout.svelte` while keeping
        `/admin/panel` built cleanly, served the page wrapped in the *root* layout, and warned
        about nothing. SvelteKit cannot detect it, so the mirror has to be right.
      - SvelteKit below 2.62.0 cannot take config inline, so exclusion falls back to output
        filtering with a warning instead of editing the user's `svelte.config.js`.

      Verified end to end on a real layered build: a `/dev` route's marker string appears 3
      times in the image built without the flag and 0 times with it, its server chunk
      (`app/server/.../entries/pages/dev/_page.svelte.js`) is gone, and the kept routes are
      untouched in both.
      Measurement, run before building it as this item asked. Three builds of a real project (static
      strategy): baseline, output filtering only, and the two demo routes deleted from source
      as a stand-in for build-time exclusion.

      | | prerendered | client JS/CSS | image (compressed) |
      |---|---|---|---|
      | baseline | 384 KB | 6912 KB | 10196 KB |
      | output filter only | 256 KB | 6912 KB | 10056 KB (-1.4%) |
      | routes removed | 256 KB | 6272 KB | 9924 KB (-2.7%) |

      So build-time exclusion is worth 132 KB more than output filtering on that project —
      1.3% of the image. The premise this item was written on ("a style guide imports the
      entire component library, so it can be one of the largest things in the image") did not
      hold there, and the reason generalises: that project is itself a component-library
      showcase, so every route imports the library and it cannot be dropped by removing two of
      five routes. The size win is real only where an excluded route is the *sole* consumer of
      something heavy. Built anyway, for the DX.
    limitations:
      - "The mirror filters the route graph, not the filesystem. A surviving route that reaches into an excluded directory by relative import still pulls that module into the bundle — the route entry points are excluded, arbitrary modules living under the excluded path are not."
      - "Falls back to output filtering (prerendered page removed, code still shipped) whenever Pokkum does not author the Vite config: a build script that does more than `vite build`, a bare `sveltekit()` whose options live in svelte.config.js, or SvelteKit below 2.62.0. The log says which mechanism applied."
      - "SvelteKit prints `svelte.config.js is ignored when options are passed via your Vite config` when config is passed inline. Cosmetic, but user-visible, and pre-existing for adapter injection."
    options:
      - title: "Stage a filtered routes tree and point kit.files.routes at it"
        description: "Mirror src/routes into .pokkum/routes as symlinks, omitting excluded paths, and set kit.files.routes to the mirror. Uses a supported config option (options.js:158) and mutates nothing the user wrote — the same shape as the existing vite-config injection."
        tradeoffs: "Relies on symlink resolution behaving: relative imports escaping the routes tree (`../../lib/x`) resolve from the link's real path under Vite's default realpath handling, which must be proven before trusting it. Excluding a parent whose children inherit its +layout must be rejected rather than silently breaking, and links to excluded routes become dead — worth a build-time warning."
      - title: "A Vite plugin that resolves excluded route modules to empty"
        description: "Intercept resolution and return an empty module for matching route files."
        tradeoffs: "Cheaper to write, but wrong in a subtle way: SvelteKit's manifest is generated independently of Vite resolution, so the routes still exist and would render blank rather than be absent. Trades a size win for a confusing runtime surface."
      - title: "Document the runtime guard and ship nothing"
        description: "Point users at the `+layout.server.ts` 404 guard."
        tradeoffs: "Zero cost and correct for access control, but leaves the code, its imports and its SBOM entries in the image — which is the part Pokkum exists to remove."
    recommendation: |
      First option, but measure before building it. Build a real project twice, once with the
      dev routes present and once with the directory removed, and compare image size and SBOM
      package counts. If a style guide really does drag in the whole component library the
      delta justifies the feature on size alone; if it is small, the honest answer is that the
      runtime guard covers most of the value and this belongs lower down.

      Whatever the size result, the attack-surface and SBOM-noise arguments stand on their own:
      a dev route that reaches privileged endpoints is worth not shipping at all, rather than
      shipping behind a guard that one refactor can remove.

      Config shape should name route paths rather than file globs (`exclude_routes: ["/dev/**"]`),
      since that is how the user thinks about them, and the guard test should assert the produced
      manifest lacks those routes rather than asserting on the filter's own bookkeeping.
    related: ["exe-secret-scan-gap", "route-exclusion-output-filter"]
    flags: ["--exclude-route"]
    impl:
      - internal/adapters/routefilterutils/mirror.go
      - internal/adapters/bunexec/route_mirror.go
      - internal/adapters/sveltekitutils/injector.go

  - id: route-exclusion-output-filter
    title: "Exclude prerendered routes from the packaged image"
    status: shipped
    kind: feature
    tier: moat
    summary: "A first phase of route exclusion: --exclude-route / build.exclude_routes drops prerendered routes from the output before packaging, and warns about links left pointing at them."
    problem: |
      [Exclude routes from the production build](item:route-exclusion-filter) is the full
      feature: filter routes before the bundler runs, so a dev-only route's code, its imports
      and its SBOM entries never enter the image. That needs a staged `kit.files.routes`
      mirror and a measurement pass to justify it, and it was not the immediate need.

      The immediate need was narrower and worth having on its own: a prerendered route that
      should not be reachable in production — a component gallery, an internal dashboard —
      still being served by the shipped image.
    decision: |
      Shipped 2026-08-22. `--exclude-route` (repeatable) and `build.exclude_routes` in
      `.pokkum.yaml` drop matching prerendered files from the output tree after the build and
      before packaging. Patterns are route paths, as the parent item recommends: a bare path
      covers its subtree, `*` matches within a segment, `**` across segments.

      Deletion goes through an `os.Root` scoped to the output tree, and the walk only collects
      matches — nothing is removed inside the `WalkDir` callback — so a symlink swapped into
      the tree cannot cause a delete outside it.

      Links from surviving pages into an excluded route warn rather than fail. Removing the
      route is the point of the flag, and failing the build would make the feature unusable
      for the case it exists to serve; a silent 404 discovered in production is the outcome
      worth preventing, and a warning does that.

      A pattern matching no prerendered route is reported rather than ignored — it is either a
      typo or a server-rendered route, and in both cases the route the operator asked to
      exclude is still in the image.

      Verified against a real project's built OCI layout: excluding a route removed its
      `index.html` and all three precompressed variants from the packaged layer, and an
      unmatched pattern warned instead of passing silently.
    limitations:
      - "This phase filters **output**, not the build: it removes the prerendered HTML while the route's JavaScript chunks, imports and SBOM entries still ship. The build-time filter that removes the code as well shipped the same day — see [Exclude routes from the production build](item:route-exclusion-filter) — so this phase is now the fallback, used where Pokkum does not author the project's Vite config."
      - "Server-rendered routes on `--strategy=layered` are compiled into the server bundle and cannot be removed by deleting a file. A pattern naming one is reported as unmatched; it is not excluded."
    flags: ["--exclude-route"]
    impl:
      - internal/adapters/routefilterutils/routefilter.go
      - internal/adapters/routefilter/adapter.go
      - internal/ports/routefilter.go
    related: ["route-exclusion-filter"]

  - id: multi-arch-oci-compilation
    title: "Zero-dependency multi-arch OCI compilation"
    status: shipped
    kind: feature
    tier: foundation
    summary: "Compiles a SvelteKit project straight into a multi-arch (linux/amd64, linux/arm64) OCI image with no Docker daemon or buildkit, using the project's configured adapter — or injecting one virtually into .pokkum/vite.config.ts when its two preconditions hold."
    flags: ["--strategy", "--platform"]
    impl:
      - internal/core/pipeline.go
      - internal/adapters/bunexec/compiler.go
      - internal/adapters/packager/packager.go
    limitations:
      - "Zero-config injection writes a transformed vite config one directory deeper (.pokkum/<viteConfigName>); __dirname/import.meta.url-derived paths and Vite's root/envDir/publicDir/build.outDir are corrected for this, but any new relative-path construct added to a future SvelteKit/Vite release needs the same audit before it can be assumed safe."
    evidence:
      commits: ["5693980"]

  - id: strategy-static
    title: "--strategy=static"
    status: shipped
    kind: feature
    tier: foundation
    summary: "Compiles a pure static SvelteKit site onto chainguard/static with an embedded pokkum-static Go file server as PID 1 — genuinely functional only since 2026-08-19, after six independent bugs were found by its first real boot test."
    flags: ["--strategy=static", "--static"]
    impl:
      - internal/adapters/staticserver
      - supervisor/cmd/pokkum-static/main.go
      - supervisor/cmd/pokkum-static/server.go
      - internal/adapters/bunexec/compiler.go
      - testdata/fixtures/sveltekit-static
    limitations:
      - "Had never worked in any prior release before 2026-08-19: both HTTP servers bound no Addr and silently fell back to port 80 (finding 2, fixed 8306d37); Preflight rejected every real adapter-static project because it hard-coded a bun/node adapter check (finding 3); real prerendered output nests under pages/dependencies/data while the code assumed a flat tree (finding 4); there was no <path>.html fallback so every non-root prerendered route 404'd (finding 7); and the embedded pokkum-static blob was gitignored and absent from every CI job and released binary until 5693980 (finding 6). All fixed in 1c33509/5693980, proven against a real @sveltejs/adapter-static fixture rather than the synthetic mock that had been encoding the same wrong flat-tree assumption."
      - "Conditional GET (If-None-Match -> 304) was missing until 61fd873 (finding 12) — every request re-downloaded the full body even when the client already held the current copy."
      - "Its own Cache-Control contract (immutable /_app/immutable, no-cache version.json/prerendered HTML) is genuinely tested here (server_test.go, integration_test.go) — see [Cache-Control contract, tested](item:cache-control-contract) for why the layered/exe strategies don't have the equivalent."
    evidence:
      commits: ["8306d37", "1c33509", "5693980", "61fd873"]
      findings: [2, 3, 4, 6, 7, 12]

  - id: runtime-node
    title: "--runtime=node, the second runtime dimension"
    status: shipped
    kind: feature
    tier: foundation
    summary: "Targets a distroless-node base and execs adapter-node output directly under /nodejs/bin/node with no Bun layer at all, proven by a real Docker boot and, since e918c52, an automated smoke test."
    flags: ["--runtime=bun", "--runtime=node"]
    impl:
      - internal/core/model.go
      - internal/core/pipeline.go
      - internal/core/runtime_node_test.go
      - tests/integration/runtime_smoke_node_test.go
    limitations:
      - "--telemetry is rejected outright for node, not silently ignored: the layered strategy's OTel bootstrap is a bun --preload mechanism with no Node equivalent."
      - "Node-core CVEs are unqueryable: distroless-node ships Node outside dpkg, invisible to the OS-package scanner, and the zero-dependency scanner has no Node-core ecosystem entry to query against OSV.dev."
      - "pokkum dev, pokkum resolve/apply, and standalone pokkum scan have zero runtime awareness — verified: no RuntimeNode reference anywhere in cmd/pokkum/dev.go, k8s.go, or scan.go."
      - "Correction to the source docs: Roadmap.md and Feature-list.md both still read, at the time of this migration, as if no automated boot smoke test existed for this runtime. That is stale — TestRuntimeSmoke_NodeRuntime_BootsAndServes (tests/integration/runtime_smoke_node_test.go) shipped in e918c52 and also asserts the *absence* of a Bun layer two independent ways, so it is real coverage, not a manual one-off."
    evidence:
      commits: ["f5229c3", "e918c52"]

  - id: rolling-deploy-asset-overlay
    title: "Rolling-deploy asset overlay (--asset-overlay)"
    status: shipped
    kind: feature
    tier: moat
    summary: "Merges the last N generations' immutable /_app/immutable client assets into a separate overlay layer, registry-side, so a browser holding a prior generation's HTML never hits a 404 mid-rollout."
    flags: ["--asset-overlay", "--asset-overlay-from"]
    impl:
      - internal/ports/assetoverlay.go
      - internal/adapters/assetoverlay
      - internal/core/pipeline.go
      - internal/adapters/packager/packager.go
      - tests/integration/asset_overlay_e2e_test.go
    limitations:
      - "pokkum verify's rebuild-and-compare path does not reproduce this layer — see [pokkum verify doesn't reproduce the asset-overlay layer](item:asset-overlay-verify-gap)."
      - "Lineage discovery is registry-side via a pokkum.dev/predecessor manifest annotation, deliberately independent of Kubernetes' pokkum.dev/image-history — a build-time flag cannot depend on cluster state without coupling build to Kubernetes. The annotation is only stamped when --asset-overlay is actually in use, so auto-discovery can only find a chain that opted in from the start of a rollout sequence."
      - "Requires --output=push for auto-discovery (there is no current tag to inspect for --local/--tarball); --asset-overlay-from's explicit refs work regardless of output mode."
    evidence:
      commits: ["f9c2f1d"]

  - id: bun-layer-diffid-stability
    title: "Bun/supervisor layer diffID stability, pinned twice"
    status: shipped
    kind: fix
    tier: foundation
    summary: "Immutable-binary layers (Bun, pokkum-init, pokkum-static) now use a fixed epoch and disable Go's VCS stamping, so their ~90MB digest stops churning on every commit for content that never actually changed."
    impl:
      - internal/adapters/packager/packager.go
      - Makefile
    limitations:
      - "This was a real bug, not a missing assertion: writing the stability test found the diffID derived its tar timestamp from SOURCE_DATE_EPOCH, which changes every commit, actively inverting what was supposed to be the single biggest fleet-wide size lever (fixed 1675d4c)."
      - "The fix was itself silently undermined until 81a6fb6: Go's default -buildvcs stamping made the pokkum-init/pokkum-static binaries' own content change every commit regardless of the tar-timestamp pin, so the two layers containing them kept churning anyway — the same failure class (build metadata leaking into a content-addressed artifact) as the first bug, a second independent source of it in the identical layers. Closed with -buildvcs=false on both embedded-binary build targets; the main CLI build deliberately keeps VCS stamping since it wants version reporting."
    evidence:
      commits: ["1675d4c", "81a6fb6"]
      findings: [17]

  - id: layered-runtime-hardening
    title: "Layered-strategy runtime hardening (stub launcher + startup attestation)"
    status: shipped
    kind: hardening
    tier: foundation
    summary: "Two composable mitigations for stock Bun's full CLI attack surface in a layered image: a non-foldable compiled entrypoint launcher, and a supervisor-verified startup digest over the /app tree."
    flags: ["--stub-launcher", "POKKUM_STUB_LAUNCHER", "POKKUM_ATTESTATION_DIGEST"]
    impl:
      - internal/adapters/bunexec/compiler.go
      - internal/adapters/packager/packager.go
    limitations:
      - "Startup attestation only exists for --strategy=layered; --strategy=exe and --strategy=static don't attest."
      - "The attested root set lives in two places that cannot share a value: ports.AttestationRoots and pokkum-init's hand-copied attestRoots (the supervisor must not import ports). Adding /app/node_modules to the packager's manifest without updating the mirror made every layered image exit 125 at startup, with the full test suite green — the build hashed 11762 files while the runtime could find 509. Both lists now carry node_modules, and TestAttestationRoots_MatchSupervisorMirror parses the supervisor's declaration with go/ast and fails on any divergence; TestAttestation_StampedDigestMatchesImageFilesystem re-derives the digest by replaying a real built image's layers rather than from the packager's own directory table, which had the same omission and so agreed with the bug."
    evidence:
      commits: ["fb23335"]

  - id: bun-release-integrity
    title: "Bun release checksum verification"
    status: shipped
    kind: hardening
    tier: foundation
    summary: "Every downloaded Bun release archive is checksum-verified before extraction — pinned digests for common versions, Bun's own GPG-signed SHASUMS256.txt.asc for anything else — failing closed rather than silently installing an unverifiable download."
    impl:
      - internal/adapters/bunruntime/resolver.go
    limitations:
      - "PB-2's first-contact gap is a stated, permanent limitation, not an open TODO: the very first resolve of a genuinely new, unlisted (version, target) on a fresh cache has no independent trust anchor beyond the GPG-signed manifest itself — GitHub's Releases API shares the same trust root as the download host and exposes no per-asset digests, so it adds no real signal. Re-running scripts/pin-bun-checksums periodically narrows this; nothing closes it fully."
    evidence:
      commits: ["0e8ae5e", "4d8ba1b"]

  - id: registry-push-and-cache
    title: "Registry push throughput, tagging, and composite remote-cache"
    status: shipped
    kind: feature
    tier: foundation
    summary: "Parallel HTTP/2 layer uploads, cross-repo blob mounting, idempotent pushes, repeatable --tag support, and a composite input hash that skips a full rebuild in sub-100ms on a verified registry cache hit."
    flags: ["--tag", "--push-concurrency", "--cache-verify", "--compression"]
    impl:
      - internal/adapters/registry/push.go
      - internal/adapters/registry/mount.go
      - internal/adapters/layercacheutils/layercacheutils.go
    limitations:
      - "Before b350ecb there was no way to tag an image at all — every build published latest unconditionally. Tags apply registry-side after the image is hashed, so the tag set never affects the digest."
      - "A remote-cache hit skips VerifyBaseImage/native inspection by design (the base digest is already bound into the cache key), but the cache-verify key chain never reads req.Signing.PublicKeyPEM, so a build signed via --signing-key alone doesn't automatically make its own cache entries independently verifiable — falls through to a full rebuild rather than failing fast with a clear story (tracked in Serena mem:open_decisions, not this file's scope)."
    evidence:
      commits: ["b350ecb"]

  - id: hermetic-build-mode
    title: "Hermetic build mode (--hermetic)"
    status: shipped
    kind: hardening
    tier: foundation
    summary: "Enforces real Linux network-namespace isolation for the build subprocess (no IP egress regardless of what a compromised dependency's build-time code tries), falling back to advisory-only isolation elsewhere."
    flags: ["--hermetic", "--hermetic-mount-isolation"]
    impl:
      - internal/core/pipeline.go
    limitations:
      - "Requires cached base image resolution, pre-populated node_modules/, and a pre-cached Bun binary — fails closed rather than downloading, so a cold cache cannot hermetic-build."
      - "--hermetic-mount-isolation's docker.sock mask has an honest residual gap: the sandboxed process retains CAP_SYS_ADMIN in its own namespace (the capability that created the mask), so a sufficiently sophisticated dependency aware of the mechanism could in principle umount() it. Closing this needs capset(2) to drop the capability before the final exec — not attempted."
    evidence:
      commits: ["28582b3", "00f791d"]

  # ---------------------------------------------------------------------
  # Open — Tier 3 (Registry & Runtime Ergonomics) and Tier 5 (Monorepo &
  # Config Drift)
  # ---------------------------------------------------------------------

  - id: asset-overlay-verify-gap
    title: "pokkum verify doesn't reproduce the asset-overlay layer"
    stage: v1.1
    status: shipped
    kind: fix
    tier: foundation
    summary: "Verifying an image built with --asset-overlay reports a false-positive digest mismatch, because verify's rebuild path has no way to re-resolve the same predecessor chain."
    problem: |
      [Rolling-deploy asset overlay](item:rolling-deploy-asset-overlay) shipped with a known,
      deliberately-not-closed gap: `pokkum verify`'s default rebuild-and-compare path has no
      `--asset-overlay`/`--asset-overlay-from` flags of its own, so it cannot reproduce the merged
      overlay layer when rebuilding for comparison. An image legitimately built with
      `--asset-overlay` therefore fails verification with an overlay-layer digest mismatch, even
      though nothing is wrong with it.
    options:
      - title: "Accept the same --asset-overlay-from refs verify's caller already knows"
        description: "Add --asset-overlay/--asset-overlay-from to pokkum verify, mirroring the build-side flags exactly."
        tradeoffs: "Simple and consistent, but only works if the caller still knows (or can reconstruct) the exact generation chain the original build resolved."
      - title: "Read the chain back off the image's own annotation"
        description: "Stamp the resolved predecessor digests onto a pokkum.dev/asset-overlay-sources annotation at build time, and have verify read it back automatically before rebuilding."
        tradeoffs: "No extra flags for the caller, self-describing — but adds another annotation to keep in sync with the actual overlay layer contents."
    recommendation: "Read the chain back off a pokkum.dev/asset-overlay-sources annotation, since it needs no caller-side bookkeeping and keeps verify's rebuild path self-contained."
    decision: |
      Option B, shipped 2026-08-19. The annotation already existed on every per-platform manifest
      of the pushed image, so only the read side was missing. `comparator.CompareImages` now reads
      it, reconstructs the merged overlay from the named predecessors through the real
      `assetoverlay` resolver, rebuilds the layer using the remote config's own `Created`
      timestamp, and only splices it into the comparison once the reconstructed DiffID equals the
      image's actual overlay-layer DiffID.

      That equality check is what makes this safe rather than merely permissive: a tampered
      annotation and a tampered layer are indistinguishable from outside, so any mismatch is
      treated as tampering. Absent annotation, malformed entry, missing reconstruction support,
      an unreachable predecessor, or an annotation with no matching overlay layer in the image's
      own history are all hard errors. Stripping the annotation buys an attacker nothing: the
      overlay layer is still there, the plain rebuild still lacks it, and the ordinary comparison
      reports L3 — proven by test, not by argument.

      A new `ports.LayerBuilder` port keeps this out of an adapter-to-adapter import.
    flags: ["--asset-overlay", "--asset-overlay-from"]
    impl:
      - internal/adapters/comparator/comparator.go
      - internal/adapters/packager/layerbuilder.go
      - internal/ports/packager.go
      - cmd/pokkum/verify.go
    limitations:
      - "Images whose only output is a local tarball carry no annotations at all, so this path cannot help them — see [Tarball output silently drops every OCI annotation](item:tarball-output-drops-annotations)."
    related: ["rolling-deploy-asset-overlay", "tarball-output-drops-annotations"]

  - id: tarball-output-drops-annotations
    title: "Tarball output silently drops every OCI annotation"
    status: shipped
    kind: fix
    tier: foundation
    summary: "pokkum build --output=tarball writes the legacy docker-save format, which has no annotations field, so every annotation Pokkum stamps is lost without warning."
    problem: |
      `internal/adapters/registry/tarball.go`'s `Write` uses go-containerregistry's
      `tarball.MultiWriteToFile`, the legacy `docker save` format. Its manifest entry carries only
      `Config`, `RepoTags`, `Layers` and `LayerSources` — there is no annotations field in the
      format at all, confirmed by reading the writer's own struct in
      go-containerregistry v0.21.9.

      Every annotation Pokkum stamps is therefore silently discarded for tarball output:
      `pokkum.dev/predecessor`, `pokkum.dev/asset-overlay-sources`, `pokkum.dev/vex-exemptions`,
      `pokkum.dev/env-baked` and the `org.opencontainers.image.*` set. Registry push is
      unaffected. Nothing warns.

      Found while implementing [pokkum verify doesn't reproduce the asset-overlay layer](item:asset-overlay-verify-gap),
      whose fix depends on reading one of those annotations back — so for an `--asset-overlay`
      image whose only output was a tarball, that fix cannot engage and the old false positive
      remains for that output mode specifically.
    options:
      - title: "Write an OCI layout instead"
        description: "Emit an OCI image layout, which has first-class annotation support, either replacing the docker-save tarball or as a second output mode."
        tradeoffs: "Correct and future-proof, and overlaps with the already-planned --to-oci-layout work — but docker load does not accept an OCI layout, so replacing the current format outright would break anyone piping into Docker."
      - title: "Warn when annotations would be dropped"
        description: "Keep the format and emit a loud warning listing the annotations being discarded whenever tarball output is used on an image that has any."
        tradeoffs: "Cheap and honest, but leaves the data genuinely lost — it converts a silent failure into a visible one rather than fixing it."
    recommendation: "Do both, in order: warn now, since a silent loss of provenance metadata is the actual defect, then fold real annotation support into the --to-oci-layout work rather than duplicating it."
    decision: |
      Warn half shipped 2026-08-19 (cd5c7f0). `Write` in tarball.go now calls a shared
      `warnDroppedAnnotations` helper (registry.go) after a successful write, naming every
      manifest annotation key actually being discarded, sorted, deterministic across runs. It
      is a deliberate no-op when the image carries no annotations at all, so an ordinary build
      stays silent. A `pokkum.dev/*` key gets its own clause calling out that other Pokkum
      commands (e.g. `pokkum verify` reading `pokkum.dev/asset-overlay-sources`) cannot work
      against this output, distinct from the purely descriptive `org.opencontainers.image.*`
      set. `--local` shares the same fix: `daemon.Write` (daemon.go) calls `tarball.Write`
      internally, so it hits the identical annotations-less docker-save format and now warns
      too — beyond this item's original scope but the same root cause, so it was folded in
      rather than filed separately.

      Second half shipped as
      [--to-oci-layout for daemonless cluster loading](item:oci-layout-dev-output), per the
      recommendation above rather than duplicated here. `--to-oci-layout <dir>` writes a
      standards-conformant OCI image layout, which has first-class annotation support: every
      `org.opencontainers.image.*` and `pokkum.dev/*` key survives on both the per-platform
      manifests and the index, and the multi-platform index itself is written faithfully instead
      of being flattened into one platform-suffixed tag per child. The layout writer therefore
      does NOT call `warnDroppedAnnotations` — there is nothing to warn about, and
      `TestOCILayout_AnnotationsSurvive_TarballLosesThem` asserts the warning's absence rather
      than leaving that to a comment. That same test runs one annotated payload through both
      writers in one place, so "annotations are preserved" is asserted against the loss it fixes
      rather than in isolation.

      `--tarball` itself is deliberately unchanged and still lossy. The format cannot carry
      annotations at all, and replacing it with a layout would break everyone piping into
      `docker load`, which does not accept one — so the two modes now coexist: the docker-save
      format for Docker compatibility, warning honestly about what it drops, and the OCI layout
      for anything that needs the metadata to survive. `--local` is in the same position for the
      same reason (the daemon's classic image store has no annotations either) and keeps its
      warning.
    impl:
      - internal/adapters/registry/tarball.go
      - internal/adapters/registry/daemon.go
      - internal/adapters/registry/registry.go
      - internal/adapters/registry/ocilayout.go
      - internal/adapters/registry/tarball_test.go
      - internal/adapters/registry/daemon_test.go
      - internal/adapters/registry/ocilayout_test.go
    limitations:
      - "The `--tarball` and `--local` outputs remain annotation-free by design; the fix is a lossless alternative mode plus an honest warning on the lossy ones, not a change to the docker-save format, which has no annotations field to write into."
      - "`pokkum verify`'s rebuild-and-compare path for an `--asset-overlay` image can now engage against an OCI layout, since `pokkum.dev/asset-overlay-sources` survives there — but an image whose only output was `--tarball` still cannot be verified that way."
    evidence:
      commits: ["cd5c7f0"]
    related: ["asset-overlay-verify-gap", "oci-layout-dev-output"]

  - id: base-name-annotation-varied-with-build-state
    title: "base.name annotation varied with local build state"
    stage: v1.2
    status: shipped
    kind: fix
    tier: foundation
    summary: "The first build of a project produced a different image digest from every later build of identical source, because `org.opencontainers.image.base.name` recorded the base reference as resolved rather than an invariant one."
    problem: |
      `org.opencontainers.image.base.name` was set from the base image reference *as resolved*, which
      the resolver rebinds with local state: to the lockfile's pinned digest form once `pokkum.lock`
      exists, and to the escrow mirror's tag when a mirror is in use. That string is baked into the
      image config, so it changes the config digest, then the manifest digest, then the index digest.

      Observable effect: the FIRST build of a project — before `pokkum.lock` existed — produced a
      different image digest from every build after it, for byte-identical source, and a build
      pulling through an escrow mirror produced a different digest from one that did not. Measured on
      the three-way benchmark: five consecutive builds, only the first differed, and the config diff
      was this one annotation with every layer and diffID identical.

      It now records `UpstreamRef`, documented as never rebound to a mirror or a locked digest form,
      which is also what the annotation is for — `org.opencontainers.image.base.digest` carries the
      digest separately and authoritatively. `BaseImageInfo.Ref` is deliberately not a fallback on
      any path, since reintroducing it anywhere reintroduces the bug.
    evidence:
      commits: []
    limitations:
      - "Breaking: base.name moves from the pinned digest form back to the upstream tag for builds that had a lockfile, so every image digest changes once. `pokkum verify` against an image built before this change correctly reports a mismatch."
    related: ["strategy-static"]

  - id: ecr-repo-autocreate
    title: "ECR repository auto-create"
    stage: v1.2
    status: open
    kind: feature
    tier: polish
    summary: "ECR requires the target repository to exist before the first push; --create-repository would close that first-push failure every ECR user hits once."

  - id: resumable-chunked-upload
    title: "Resumable chunked layer upload"
    stage: v1.2
    status: open
    kind: hardening
    tier: polish
    summary: "Back off and retry on 429/5xx during a large layer push instead of failing the whole push on one transient registry hiccup."

  - id: registry-error-surfacing
    title: "Registry-specific error surfacing"
    stage: backlog
    status: open
    kind: dx
    tier: polish
    summary: "Translate GAR/Harbor project-path errors and Docker Hub anonymous-pull rate limits into a readable, specific message instead of a generic push/pull failure."

  - id: supervisor-cgroup-awareness
    title: "Supervisor cgroup awareness"
    stage: v1.2
    status: open
    kind: feature
    tier: polish
    summary: "JSC (Bun's engine) doesn't read cgroup limits, so a Bun app in a 512Mi container OOMKills in ways that look random; read /sys/fs/cgroup/memory.max, export it, and warn below a sane floor."

  - id: source-maps-oci-referrer
    title: "Source maps as an OCI referrer"
    stage: backlog
    status: open
    kind: feature
    tier: polish
    summary: "Strip source maps from the shipped image and attach them as a digest-keyed OCI referrer artifact, so Sentry release tagging works without shipping maps to production."

  - id: cache-control-contract
    title: "Cache-Control contract, tested for every strategy"
    stage: v1.2
    status: open
    kind: infra
    tier: foundation
    summary: "The /_app/immutable, version.json, service-worker.js and prerendered-HTML header contract is a tested invariant only for --strategy=static today; layered/exe rely on adapter-node's bundled sirv defaults with no Pokkum-side test."
    problem: |
      Getting this contract wrong either breaks SvelteKit's own `updated.check()` polling or serves
      stale HTML forever. `supervisor/cmd/pokkum-static/server_test.go` and `integration_test.go`
      already assert it precisely for [--strategy=static](item:strategy-static). The layered and
      exe strategies serve prerendered content through adapter-node's bundled `sirv`, which sets its
      own defaults — nothing in this codebase currently asserts they match the intended contract
      (`public, max-age=31536000, immutable` for hashed assets; `no-cache` for `version.json`,
      `service-worker.js`, and prerendered HTML).
    related: ["strategy-static"]

  - id: monorepo-vendor-cache
    title: "Shared vendor cache across a monorepo invocation"
    stage: v1.2
    status: open
    kind: feature
    tier: foundation
    summary: "--since skips builds between unaffected projects but does nothing within a build when many packages in one invocation share dependencies; extending the layer cache into a content-addressable vendor-layer cache would close that."
    problem: |
      Extend `layercacheutils` into a cache keyed by `package.json` + lockfile subtree, shared
      across every project built in one invocation — analogous to how `ko` leans on Go's build
      cache. Confirmed by reading `internal/adapters/layercacheutils/layercacheutils.go`: the
      existing cache has no cross-project sharing concept today, so this gap is real and not
      merely asserted from the feature list, as the original roadmap note asked to verify.
    flags: ["--since"]

  - id: doctor-drift-check
    title: "pokkum doctor drift check"
    stage: backlog
    status: open
    kind: dx
    tier: polish
    summary: "Nothing currently validates that a .pokkum.yaml's configured adapter, base image, and telemetry settings are still coherent after a SvelteKit or Bun upgrade."

  - id: scoped-secret-allow-annotations
    title: "Scoped secret-allow annotations"
    stage: backlog
    status: shipped
    kind: feature
    tier: polish
    summary: "--allow-secret-pattern is a global regex; an inline pokkum:allow-secret comment gives a known-safe line the scoped exemption it actually needs."
    decision: |
      Shipped 2026-08-19, the inline-comment half. A line carrying a comment that contains
      `pokkum:allow-secret` — on the line itself or the one directly above — is skipped. The marker
      is matched as a substring, so `//`, `#`, `/* */` and `<!-- -->` all work with no per-language
      knowledge in the scanner.

      Prompted by a real false positive: a sanitizer whose job is redacting secrets was flagged for
      containing the literal `password: '[REDACTED]'` in its replacement strings. Three findings in
      one function, all of them the tool matching its own redaction placeholders.

      Why a marker rather than a pre-filled regex, which is what was asked for: an allow pattern
      matches a whole *line*, so generating one means printing the line — and if the finding is a
      genuine secret rather than a placeholder, that suggestion is the secret, in a log and then in
      a committed config file. The scanner cannot tell those apart, so suggesting a pattern built
      from content would leak by default in exactly the case that matters. A marker describes
      nothing.

      Why not a file:line suppression, the other option considered: line numbers shift, so the
      exemption silently moves to whatever code arrives at that line — the reporter identified this
      objection themselves. A marker travels with the line it exempts.

      Both mechanisms are named in the failure output, because neither covers everything: a marker
      cannot be added to generated output, so a minified bundle carrying redaction strings compiled
      from annotated source still needs `security.allow_secret_patterns` (which already existed and
      was merely undiscoverable). Verified on the real case: three findings to zero via markers, and
      three to zero via a `\[REDACTED\]` pattern.

      The `.secretguardignore` variant is deliberately not implemented. Path-scoped exemption is
      the blunter tool, and patterns already cover the generated-output case that motivated it.
      Scope is deliberate: an exemption is line-scoped, covering its own line and the one below,
      proven by a test that marking one redaction line leaves the other two flagged.

      Threat model, stated rather than implied: anyone who can add the marker can add the secret
      itself, so it grants no new capability. It records intent; it is not a boundary.
    flags: ["--allow-secret-pattern"]

  - id: build-time-test-gate
    title: "Build-time test gate (--test)"
    stage: backlog
    status: open
    kind: feature
    tier: polish
    summary: "Condition image creation on the project's own test suite passing, once its interaction with --hermetic (a test suite needing network access conflicts with hermetic-by-default) has an explicit answer."
    flags: ["--hermetic"]

  - id: precompress-fanout-race
    title: "Concurrent per-platform builds race on precompressed sidecars"
    stage: v1.1
    status: shipped
    kind: fix
    tier: foundation
    summary: "Every platform re-runs precompression over the same output directory while other platforms are tarring it, so a multi-arch build can fail with archive/tar write too long."
    problem: |
      `fanOut` runs the per-platform compile/supervisor/package chain **concurrently**, and each
      platform's `appendPrerenderedLayer` calls
      `precompressutils.PrecompressDirectory(req.AppPrerenderedDir, ...)` against the **same**
      directory, then immediately walks that directory to build the layer.

      `PrecompressDirectory` writes each sidecar with `os.WriteFile`, which truncates before
      writing. So platform A can stat `index.html.br` while platform B is mid-rewrite, take the
      partial size into the tar header, then read the completed file — writing more bytes than the
      header declared. `archive/tar` rejects that outright:

        packager: build linux/arm64: prerendered layer: write tar entry
        "app/prerendered/index.html.br": archive/tar: write too long

      This is not test-only. The default platform set is `linux/amd64,linux/arm64`, so any
      multi-arch build of a project with prerendered pages runs exactly this pattern. It is
      intermittent, which is worse than deterministic: it presents as a flaky build.

      Observed as `TestFixtureDrivenE2E_Static_SPAFallback` failing under full-suite parallelism
      and passing in isolation. It was **previously misattributed** — including by me — to load
      contention from two test packages that were hanging on a Docker credential helper. Fixing
      those hangs made it stop reproducing for a while, which looked like confirmation and was
      coincidence. `-race` cannot see it: the race is mediated by the filesystem, not memory.

      Two adjacent defects in the same lines: `PrecompressDirectory`'s error is discarded
      (`_ = precompressutils...`), so a genuine precompression failure is silent; and the work is
      duplicated per platform, compressing identical bytes N times for N platforms.
    options:
      - title: "Precompress once, before the fan-out"
        description: "Move precompression ahead of fanOut so it runs a single time over the shared output tree, and have each platform's packager only read the sidecars."
        tradeoffs: "Correct and strictly less work — precompression output does not depend on the target platform, so per-platform execution was never meaningful. Requires moving the step across the core/adapter boundary, so the packager must no longer assume it may generate sidecars itself."
      - title: "Serialise precompression per directory"
        description: "Guard PrecompressDirectory with a per-path mutex or sync.Once so only one platform generates sidecars while the others wait."
        tradeoffs: "Smaller diff and keeps the call where it is, but leaves the duplicated-work problem and keeps a shared-mutable-directory design that the next concurrent consumer can trip over again."
    recommendation: "Precompress once before the fan-out. The per-platform call is not just unsafe but meaningless — the bytes are identical for every platform — so hoisting it fixes the race and removes N-1 redundant compressions rather than merely making the collision orderly."
    decision: |
      Shipped 2026-08-19 — but not by hoisting, and the reason is a hard constraint the
      recommendation missed: `internal/core` may not import a `*utils` package (enforced by
      `TestUtilityPackagesNotImportedFromCoreOrPorts`), so core cannot call precompression at all
      without introducing a new port. That is a larger change than the defect warrants.

      What shipped instead achieves the same two effects inside the adapter:

      1. **Freshness made meaningful.** A sidecar's on-disk mtime now comes from its source rather
         than the build epoch. `isStale` compares those two, and pinning sidecars to the epoch while
         a build writes its sources *now* made every sidecar permanently stale — so every platform
         re-ran brotli at BestCompression over the whole tree. Safe for reproducibility: the only
         `ModTime` that reaches a tar header is the pinned value `writeTar` receives, so on-disk
         mtimes never influence image bytes. This removes the N-1 redundant compressions the
         recommendation wanted.
      2. **Per-directory serialisation.** Precompressors no longer overlap each other.

      Together they give the invariant the race needed: **writes never overlap a walk.** The first
      platform writes while the others block on the lock; every later platform then finds the
      sidecars fresh and writes nothing; so all writing has finished before any tar walk begins.

      The three discarded `_ =` errors now warn through the packager's logger — a precompression
      failure previously shipped a slower image with no signal.

      **Two rejected approaches, recorded because both looked better than they were.** Atomic
      writes (temp file plus rename) seemed strictly safer and are wrong here: `os.CreateTemp` puts
      the temporary file in the very directory the packager walks, so a concurrent walk either fails
      its lstat when the file is renamed away or packages a `.tmp-*` file into the image — caught by
      a test. And a test forcing rewrites during a walk was written, failed intermittently, and was
      deleted: it asserts write atomicity, a guarantee this design deliberately does not provide.
    limitations:
      - "The race has no reproducing test. Four designs were tried; the vulnerable window is roughly one syscall wide and `-race` is blind to a filesystem-mediated race. The guard is instead the freshness test — if later passes ever start rewriting again the ordering argument collapses and it fails, verified 12/12 against the reverted fix."
      - "Safety rests on freshness detection being correct. A source genuinely modified mid-build would be recompressed by a later platform while an earlier one tars; that does not happen in a normal build, and is stated in the code rather than assumed away."
    impl:
      - internal/adapters/precompressutils/precompressutils.go
      - internal/adapters/packager/packager.go
      - internal/core/pipeline.go
    related: ["strategy-static", "cache-control-contract"]


  - id: extra-project-paths
    title: "build.extra_paths — ship project directories beyond the adapter's output"
    stage: backlog
    status: open
    kind: feature
    tier: polish
    summary: "Only the adapter's `build/` output enters the image, so a project reading templates, fonts or seed data off disk has to smuggle them in via a postbuild copy."
    problem: |
      Pokkum packages the adapter's output and nothing else from the project root. That is the
      right default — it is most of why the images are small — but it has no escape hatch, and
      the class of application that needs one is not exotic: anything reading a template
      directory, a font set, or seed data directly off disk.

      The workaround that does work today is a postbuild script copying those directories into
      `build/` so they ride along in the existing server layer, plus setting `image.working_dir`
      to the path that layer mounts at so the app's own `process.cwd()`-relative resolution keeps
      working unchanged. It is two moving parts held together by a convention nothing enforces,
      and neither part is discoverable — it was reconstructed from first principles in a real
      session (2026-09).
    recommendation: |
      A `build.extra_paths` list, copied to a documented stable path under the app tree and
      surfaced to the application through an environment variable so it does not have to infer
      the location from `working_dir`. Determinism is unaffected: the same sorted-walk and
      `SOURCE_DATE_EPOCH` header rules the existing layers use apply unchanged.

      Deliberately narrower than [extra OS binaries](item:extra-os-binaries): the content is the
      operator's own source tree, already inside the build context and already covered by
      `secretguard` and the SBOM, so it opens no new supply-chain surface. That is what makes it
      the one of the two pair worth doing first.
    related: ["extra-os-binaries", "readonly-app-invariant-undocumented", "vendor-production-dependencies"]

  - id: extra-os-binaries
    title: "Vendoring a third-party OS binary into the image (Typst, pandoc, ffmpeg)"
    stage: backlog
    status: awaiting-decision
    kind: feature
    tier: polish
    summary: "A SvelteKit app shelling out to a real CLI tool currently needs a hand-built custom base and a separate buildx pipeline, which undercuts the zero-Dockerfile pitch for that whole class of app."
    problem: |
      "SvelteKit app that invokes a native tool" is a common shape — Typst, pandoc, ImageMagick,
      ffmpeg. Today it requires building a custom base image by hand (`FROM <tool image>` /
      `FROM <resolved preset digest>` / `COPY --from`), which means running `pokkum base update`
      to discover the preset's real reference, keeping a Dockerfile and a buildx builder
      configured for multi-platform, and re-verifying the tool's tag still resolves on both
      architectures. Done end to end in a real session (2026-09); it worked, and it was most of
      the work.

      The machinery is largely present. `bunruntime` already downloads a GitHub-release binary,
      verifies its checksum, caches it and embeds it as its own deterministic layer — a Typst
      resolver is close to that adapter with different URLs. The supervisor is runtime-agnostic.
      The packager, multi-arch index and the whole attestation chain sit above the application
      type, not inside it.
    options:
      - title: "Do not build it"
        description: "Keep Pokkum a SvelteKit compiler. Document the custom-base recipe properly in the guide and leave the tool image to a Dockerfile, apko or ko."
        tradeoffs: "Preserves the product boundary and the supply-chain story exactly. Leaves a common application shape needing a second toolchain, which is the thing Pokkum exists to remove."
      - title: "build.extra_binaries sourced from an upstream image"
        description: "A config list of {from, path, to}, compiled as an extra COPY --from layer, as suggested from the field."
        tradeoffs: "Narrower than a general image builder — no second Compiler, no reshaped PackageRequest. But it lets arbitrary bytes from an arbitrary image into the image while bypassing base-image verification, and the scanner and SBOM would need to cover a layer whose provenance nothing established."
      - title: "A first-class tool resolver, modelled on bunruntime"
        description: "Named, version-pinned, checksum-verified resolvers for a small set of tools, resolved from upstream releases rather than from another container image."
        tradeoffs: "Keeps verification and reproducibility intact and fits the existing adapter shape. Costs a maintained resolver per tool, and does not generalise — every new tool is a code change."
    recommendation: |
      Hold at awaiting-decision, and ship [extra_paths](item:extra-project-paths) first: it
      covers the adjacent half of the same field report at a fraction of the risk, and it will
      show whether the binary case is really the common one or was incidental to a single
      project.

      If it is built, the constraint that decides the design is supply chain, not plumbing. An
      arbitrary `COPY --from` is a hole straight through the base-image verification, CVE gate
      and SBOM coverage that are the reason to use this tool at all — so any source image must
      pass the same verification path a `--base` custom reference already does
      ([custom-base-lock-slot](item:custom-base-lock-slot),
      [base-image-signature-verification](item:base-image-signature-verification)), get its own
      lock slot, and be scanned. Under that constraint the third option is closer to Pokkum's
      grain than the second, and the second is only acceptable if it fails closed by default the
      way a custom base does.

      Explicitly out of scope either way: building images for non-SvelteKit inputs. That changes
      what Pokkum is, and the field report's own conclusion — build the tool image separately,
      keep Pokkum for the app — remains the right answer until a second real use case exists.
      `pokkum resolve`/`apply` already patch image references by container name, so a sidecar in
      a hand-written manifest survives a deploy untouched; what Pokkum cannot do is generate that
      sidecar, since only the OTel one is injectable.
    related: ["extra-project-paths", "custom-base-lock-slot", "base-image-signature-verification", "chainguard-static-preset", "agent-guide-command"]
