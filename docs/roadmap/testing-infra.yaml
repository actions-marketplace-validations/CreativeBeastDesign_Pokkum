area: "Testing & Infrastructure"
items:

  - id: sbom-determinism
    title: "SBOMs of identical source were not identical"
    status: shipped
    kind: fix
    tier: foundation
    summary: "Every npm-family lockfile parser resolved duplicate package names by Go's randomized map order, so two builds of unchanged source produced different package sets, versions and SBOM digests."
    problem: |
      A lockfile routinely records one package name at several versions: a hoisted copy keyed
      by the bare name, plus nested copies keyed by their dependency path. The package
      catalogue is keyed by name and holds one of them, and all four parsers
      (`bun.lock`, npm v2, npm v1, pnpm) picked the winner by ranging over the parsed map —
      which Go deliberately randomises.

      Because the same loop also builds the reachability graph that assigns
      production/development scope, the effect was not limited to versions: it changed how
      many packages the document contained at all. Six builds of one unchanged project
      produced 9, 10, 11, 13, 14 and 15 packages, and six SBOM documents with six different
      SHA-256s.

      For a tool whose central claim is bit-for-bit reproducibility, the supply-chain artefact
      was the part that moved.
    decision: |
      Shipped 2026-08-22. Fixed at the collision rather than at the output: iterate keys in
      sorted order, and make the winner an explicit rule — the hoisted copy wins, because it
      is the one at `node_modules/<name>` that a bare import actually resolves to.

      Why it survived review is the part worth recording. The output was carefully sorted,
      with a comment explaining that package order must be deterministic. It was. Sorting a
      list assembled by a nondeterministic *selection* yields a stably-ordered list of
      unstable contents, and looks exactly like the fix.

      Regression tests run 200 iterations per lockfile format, because a two-entry collision
      passes a single run about half the time, and assert both stability and that the
      semantically correct duplicate won. All four fail against the previous parsers.
      Verified end to end: six SBOM generations over a real project now produce one identical
      digest.
    impl:
      - internal/adapters/scannerutils/scannerutils.go
  # ---------------------------------------------------------------------
  # Shipped
  # ---------------------------------------------------------------------

  - id: runtime-boot-smoke-tests
    title: "Real Docker boot smoke tests"
    status: shipped
    kind: infra
    tier: foundation
    summary: "The first tests in this repo to actually boot a produced image and poll it, for both the default layered/Bun strategy and --runtime=node, instead of only proving the packaged bytes are right and stable."
    impl:
      - tests/integration/runtime_smoke_test.go
      - tests/integration/runtime_smoke_node_test.go
    limitations:
      - "Every existing layer-structure/determinism/golden-manifest test had proven the packaged bytes were correct and stable, never that the result actually runs — that gap is exactly what let every layered image ship without a working entrypoint for this codebase's entire prior history (see Lessons.md's 2026-08-18 entry on the missing /app/server/index.js)."
      - "Gated on -short/bun/docker/network, each skipping cleanly rather than failing when unavailable."
      - "--strategy=static has its own separate fixture-driven boot test rather than this harness — see [Real @sveltejs/adapter-static test fixture](item:static-strategy-real-fixture)."
    evidence:
      commits: ["fc53968", "e918c52"]

  - id: fuzz-targets
    title: "Fuzz targets across parsers and codecs"
    status: shipped
    kind: infra
    tier: foundation
    summary: "Fuzz targets now exercise DSSE PAE encoding, ignore-pattern matching, Cosign payloads, asset-overlay merge logic, Bun SHASUMS parsing, scanner version comparison, and the static file server — none of these existed before."
    impl:
      - internal/adapters/dsse/pae_fuzz_test.go
      - internal/adapters/ignoreutils/matcher_fuzz_test.go
      - internal/adapters/cosign/payload_fuzz_test.go
      - internal/adapters/assetoverlay/assetoverlay_fuzz_test.go
      - internal/adapters/bunruntime/shasums_fuzz_test.go
      - internal/adapters/scanner/version_fuzz_test.go
      - supervisor/cmd/pokkum-static/server_fuzz_test.go
    evidence:
      commits: ["e6e4746"]

  - id: race-detector-and-coverage-floor
    title: "Race detector + enforced coverage floor"
    status: shipped
    kind: infra
    tier: foundation
    summary: "go test -race now runs in CI over the packages where concurrency actually lives, and coverage is measured and enforced at a 75% floor as a ratchet off the real baseline (77.8% measured 2026-08-20), not an aspirational number."
    impl:
      - scripts/check-coverage.sh
      - Makefile
    limitations:
      - "-race is deliberately scoped to registry/core/packager/supervisor rather than the full ./... tree, to keep the added CI cost (~6s) proportionate to where concurrency actually lives."
      - "Landed in the same change as fixing a structural CI blind spot: CI never installed Bun before this, so every genuinely-real-build e2e test silently skipped and CI's 'e2e' job was entirely mock-compiler. A separate e2e-real-build job now installs Bun, kept apart so the fast hermetic gate stays fast."
    evidence:
      commits: ["e6e4746"]

  - id: cli-docs-invariant-tests
    title: "CLI/docs drift as a mechanical test failure"
    status: shipped
    kind: infra
    tier: foundation
    summary: "Five new tests check Vocabulary.md and action.yml against the CLI's real flag/env-var surface in both directions, in the spirit of internal/architecture_test.go, and found three genuine drifts on their first run."
    impl:
      - cmd/pokkum/flags_docs_test.go
      - cmd/pokkum/envvar_docs_test.go
      - cmd/pokkum/actionyml_test.go
    limitations:
      - "Found and fixed on first run: POKKUM_LOG_LEVEL (read by both PID-1 binaries) was undocumented, --write-config on adopt was undocumented, and Vocabulary.md claimed a verify --rebuild flag that does not exist (the real behavior is rebuild-by-default with --no-rebuild to opt out)."
      - "The same commit closed six merged-but-unvalidated .pokkum.yaml config fields, including profiles.<name>.output, which nothing had validated before."
    evidence:
      commits: ["548c0e1"]

  - id: embedded-blob-freshness-guard
    title: "Embedded PID-1 binary freshness guard"
    status: shipped
    kind: infra
    tier: foundation
    summary: "make check-embedded-blobs rebuilds pokkum-init and pokkum-static fresh from source with the exact Makefile flags and byte-compares them against what is actually embedded, catching local staleness a from-scratch CI build structurally cannot hit."
    impl:
      - internal/adapters/staticserver/blob_freshness_test.go
      - Makefile
    limitations:
      - "Must run with -count=1: go test's result cache cannot see through an exec'd go build into another package's source, so a stale cached result would otherwise report clean."
      - "Deliberately not added to the PR-gate CI job — CI always rebuilds both blobs from the checked-out commit before any test runs, so this specific staleness is structurally impossible there. It exists for the local working-tree hazard, which is exactly where it was first found live (concurrent commits had moved HEAD past the last local rebuild)."
    evidence:
      commits: ["a86baa3"]

  - id: fixture-isolation
    title: "Real-build tests copy their fixture into t.TempDir() first"
    status: shipped
    kind: infra
    tier: foundation
    summary: "Every mutating real-build test now copies its checked-in fixture into a fresh t.TempDir() before building, closing an order-dependence that had already caused three separate incidents, proven at -count=3 -shuffle=on."
    impl:
      - tests/integration/harness_test.go
      - internal/adapters/bunexec/integration_test.go
    limitations:
      - "Stale-claim correction: overnight-findings.md's finding 11 recorded this as 'not fixed — deliberately,' calling it a broader test-hygiene change than belonged at the end of that queue. It was, in fact, done the same day. Serena's mem:state, as read at the start of this migration, still described it as 'known fragility, deliberately not fixed yet' — also stale. Commit 20ba1ec generalized the t.TempDir()-copy pattern tests/integration/runtime_smoke_test.go had already established to all five affected real-build tests, moved the shared helper into harness_test.go, and proved order-independence empirically rather than asserting it."
      - "Read-only tests were deliberately left untouched, but only after confirming — by reading the actual production code, not assuming — that nothing in their dependency chain (sbom.Generator, packager.Packager, mockCompiler's StrategyExe branch) ever calls os.WriteFile/os.MkdirAll against ProjectDir."
      - "bunexec cannot import the tests/integration helper (a separate package, and adapter-to-adapter imports are architecturally forbidden), so it carries a small, deliberate duplicate that — unlike the shared helper — does not skip .svelte-kit, since that test's precondition is a pre-prepared fixture."
    evidence:
      commits: ["ac5dc89", "20ba1ec"]
      findings: [11]

  - id: static-strategy-real-fixture
    title: "Real @sveltejs/adapter-static test fixture replaces a fictional mock"
    status: shipped
    kind: infra
    tier: foundation
    summary: "A genuine, scaffolded-and-built @sveltejs/adapter-static project replaced a synthetic mock that had been fabricating a flat prerendered/index.html, immediately exposing four bugs the mock's own wrong assumption had been hiding."
    impl:
      - testdata/fixtures/sveltekit-static
      - tests/integration/static_e2e_test.go
    limitations:
      - "This is the sharpest instance of a recurring lesson in this codebase: a mock encoding the same wrong assumption as the code it tests can never detect the mismatch. TestFixtureDrivenE2E_Static had passed throughout, because both the mock and the production code shared the same incorrect belief that prerendered output is a flat tree — see [--strategy=static](item:strategy-static) in build-packaging.yaml for the bugs this found (findings 2, 3, 4, 6, 7)."
    evidence:
      commits: ["0ef9dd0", "1c33509"]
      findings: [4, 6, 7]

  - id: walk-callback-helper-delegation-toctou
    title: "Helper-delegated walk callbacks are outside G122's reach"
    status: open
    kind: infra
    tier: polish
    summary: "12 of the repo's 15 Walk/WalkDir callbacks pass the walked path to a helper that opens it internally, so the symlink-TOCTOU class survives in a form gosec G122 structurally cannot see."
    problem: |
      [Root-scoped filesystem APIs in filepath.Walk callbacks (gosec G122)](item:walk-callback-symlink-toctou)
      converted the five callbacks that called `os.*` on the walked path directly, and re-armed
      G122. That check is intraprocedural: it sees `os.ReadFile(p)` inside the callback, and does
      not see `helper(p)` where the helper does the same thing one frame down.

      Enumerating all 15 Walk/WalkDir callbacks found 12 in the second shape — the walked path is
      handed to `striputils.StripELFFile`, `precompressutils.PrecompressFile` (which stats and
      reads it internally), or `sveltekitutils.ReadPackageJSON`/`ResolveVersion` on
      `filepath.Dir(p)`. The exposure is the same class as the converted sites, and the linter
      reports zero issues on all of them.

      This is not an argument that the conversion was pointless: the converted sites are now
      structurally contained, and the armed check does prevent the *direct* shape from returning.
      It is a statement of what "G122 armed, 0 issues" actually buys, so nobody reads it as
      "walk callbacks in this repo cannot be TOCTOU'd".
    options:
      - title: "Thread an os.Root through the helper APIs"
        description: "Change the shared helpers to accept an *os.Root plus a root-relative path instead of an absolute path, so containment is a property of the signature."
        tradeoffs: "Eliminates the class and makes it unrepresentable, but changes a shared API used by many callers across ~10 packages, including internal/adapters/secretguard, and is a genuinely large diff."
      - title: "Convert only the walks over trees an attacker could plausibly influence"
        description: "Triage the 12 by what they traverse — a dependency-writable node_modules tree is interesting, the image's own layers at boot much less so — and convert that subset."
        tradeoffs: "Much smaller and targets real exposure, but leaves the class present and the linter still blind to it, so the next such callback is unguarded."
      - title: "Accept and document"
        description: "Record that these walks traverse trees the build already trusts, and leave them."
        tradeoffs: "Zero cost; relies on every future walk callback author knowing the distinction, which is what row 22 exists because people do not."
    recommendation: |
      Second option first: triage by what is actually traversed, and convert the build-time walks
      over dependency-writable trees. The first option is the real fix but should not ride along
      with anything else — it is a shared-API change, and the fact that it touches secretguard
      means it needs the same care the PID-1 conversion just got.
  - id: walk-callback-symlink-toctou
    title: "Root-scoped filesystem APIs in filepath.Walk callbacks (gosec G122)"
    status: shipped
    kind: infra
    tier: polish
    summary: "Five filepath.Walk/WalkDir callbacks operate on the walked path directly, which gosec G122 flags as symlink-TOCTOU-prone; os.Root would make the containment structural rather than incidental."
    problem: |
      Upgrading to golangci-lint v2 enabled gosec's G122 check, which flags a filesystem
      operation inside a `filepath.Walk`/`WalkDir` callback that acts on the callback's path:
      between the walk observing a directory entry and the callback opening it, that path can
      be replaced with a symlink pointing outside the tree. Five sites are affected —
      `internal/adapters/bunexec/prerendered_flatten.go`, `internal/adapters/sbom/generator.go`,
      and `supervisor/cmd/pokkum-init/attest.go`.

      Unlike the taint-analysis checks excluded alongside it (G702/G703), this one is not a
      false positive: the containment is incidental rather than structural. In practice the
      exposure is low — these walks traverse either the user's own project directory at build
      time or the image's own layers at boot, not attacker-chosen trees — which is why it was
      excluded in `.golangci.yml` rather than rushed, but "low" is not "none", and a
      build-time walk over a directory a dependency can write into is the interesting case.

      `os.Root` (Go 1.24+) makes this structural: operations resolve against a root handle and
      cannot escape it, so the guarantee no longer depends on every future caller remembering
      the check. See mem:self_review_checklist row 22 on containment checks.
    limitations:
      - "One deliberate behaviour change: `os.Root` refuses an **absolute** symlink target even when it names a path inside the root, because openat-based resolution cannot prove it. Relative in-root symlinks are unaffected. Impact is nil for the attest site (symlinks are filtered before the read) and negligible for the other two, and it is pinned by a test rather than left to be rediscovered as a bug."
      - "Second, smaller change: `sbom.scanProject` now errors when `ProjectDir` is not a directory (`OpenRoot` returns ENOTDIR) where it previously produced an empty package list. Unreachable from `pokkum build`."
      - "**G122's analysis is intraprocedural, so \"armed and 0 issues\" means no walk callback performs a direct unscoped filesystem operation — not that no walk callback can be TOCTOU'd.** All 15 Walk/WalkDir callbacks were enumerated; the 12 unflagged ones hand the walked path to a helper (`striputils.StripELFFile`, `precompressutils.PrecompressFile`, `sveltekitutils.ReadPackageJSON`) which performs the operation internally. Same class, structurally invisible to the linter — tracked as [Helper-delegated walk callbacks are outside G122's reach](item:walk-callback-helper-delegation-toctou)."
    options:
      - title: "Convert the five sites to os.Root"
        description: "Open an os.Root for the walk base and use root-relative operations inside each callback, then drop the G122 exclusion from .golangci.yml so the check guards new code."
        tradeoffs: "Removes the whole class and re-arms the linter, but touches three packages including the PID-1 attest path, which ships inside the image and has the least tolerance for churn."
      - title: "Convert the build-time walks only, keep the exclusion for supervisor/"
        description: "Harden bunexec and sbom, where a dependency-writable tree is plausible, and leave the in-image attest walk as-is."
        tradeoffs: "Cheaper and targets the reachable case, but leaves the exclusion in place repo-wide, so the check still cannot guard new code."
    recommendation: |
      Take the first option, but as its own change rather than folded into unrelated work: the
      diff is mechanical yet spans a security-sensitive PID-1 binary, and the payoff is
      re-arming G122 so it guards future walk callbacks instead of being permanently muted.
    decision: |
      Option one shipped 2026-08-20. All five findings converted to `os.Root`, and G122 removed
      from `.golangci.yml`'s gosec excludes so it now guards new walk callbacks: lint is 0 issues
      with the check armed, and reverting any one site makes it fire again (proven for the sbom
      site: 3 findings).

      The reachable escape was more than theoretical at one site. `prerendered_flatten` moves
      files up one level, and while the walked relative path cannot contain `..`, a symlinked
      *destination* parent had `os.Rename` deposit a prerendered file outside the staging tree
      and return nil. Containment tests assert on canary bytes outside the tree, not on error
      values, because the old code returned no error at all.

      For the PID-1 attest site the escape is not statically reachable through the walk —
      `WalkDir` never follows symlinks and the `IsRegular` filter drops every symlink it
      reports — so old and new code produce identical digests for any static tree. The
      vulnerable window is one syscall wide and not reproducible on demand, so the observable
      was moved to the read primitive, where old and new genuinely differ, and the walk-level
      test asserts the invariant the design maintains instead. That choice is written into the
      test file so it is not "fixed" later by re-adding a test for a guarantee deliberately not
      made.

      Verified beyond the standard suite because this binary ships inside the image: a real
      container booted with the rebuilt `pokkum-init`, and the `os.Root` walk re-derived a
      startup digest matching the packager's build-time manifest over 79 files.
  - id: hermetic-privileged-ci-step
    title: "Hermetic Mount Isolation tests in privileged CI container"
    status: shipped
    kind: infra
    tier: foundation
    summary: "The `--hermetic-mount-isolation` security control is exercised by end-to-end tests that bind-mount isolation barriers over hermetic-sensitive paths; these tests cannot run on unprivileged GitHub-hosted ubuntu runners, so they run in a privileged Docker container in the e2e-real-build CI job, alongside real-build integration tests."
    decision: |
      Shipped 2026-08-19. The control `--hermetic-mount-isolation` is a build-sandbox security feature
      that uses bind-mount masking to prevent layer assembly from accessing sensitive paths. Its two
      end-to-end tests (`TestPrepare_HermeticMountIsolation_EndToEnd` and
      `TestHermeticMountIsolation_BlocksPathSocketAccess` in `internal/adapters/bunexec`) must execute
      as root or in a privileged container to bind-mount the isolation barriers.

      GitHub-hosted ubuntu runners do not grant mount privileges. Setting
      `kernel.apparmor_restrict_unprivileged_userns=0` was attempted but did not help — the sysctl
      applied cleanly, yet the bind-mount still returned EPERM, indicating the blocker is the runner's
      mount privileges generally, not AppArmor's unprivileged-userns restriction specifically.

      Resolution: The `e2e-real-build` CI job now includes a step named "Hermetic Sandbox Tests
      (privileged container)" that runs `docker run --rm --privileged ... golang:<go.mod version> go test
      -count=1 -run 'Hermetic' ./internal/adapters/bunexec/`. This brings the control from "skipped
      everywhere" to "tested 14/14 with 0 skips". The tests pass on the ephemeral GitHub-hosted VM
      inside a privileged container, and they also pass locally on developer machines (verified in
      rootless and privileged Linux Docker environments). The skip path remains as a fallback for
      developer systems that cannot run privileged containers.

      The accepted risk: CI is triggered on `pull_request` (see `.github/workflows/ci.yml` on: block),
      so a fork PR's code executes inside a privileged container. The user explicitly chose to accept
      this. Rationale: fork PRs already run arbitrary code on an ephemeral GitHub-hosted VM with a
      read-only token, and that job holds no credentials or secrets; --privileged adds container-escape
      reach, but the VM is disposable and has no durable state. No net increase in blast radius.
    impl:
      - .github/workflows/ci.yml
      - internal/adapters/bunexec/hermetic_reexec_linux_test.go
    limitations:
      - "Fork PRs execute code in a privileged container. Revisit if: (a) any secret or credential is ever added to the e2e-real-build job, (b) a self-hosted runner becomes available that can grant the mount capability without --privileged, or (c) GitHub runners ever permit unprivileged-userns bind-mounts."
      - "The sysctl kernel.apparmor_restrict_unprivileged_userns=0 was diagnosed as the blocker and attempted; it did not help, which is documented in the commit message to save future investigation of the same dead end."
    evidence:
      commits: ["2b9da12", "0339ec6"]

  # ---------------------------------------------------------------------
  # Open
  # ---------------------------------------------------------------------

  - id: verify-suite-scope-gaps
    title: "make verify's five steps don't cover supervisor/ or the integration/golden test suites"
    stage: backlog
    status: open
    kind: infra
    tier: polish
    summary: "supervisor/ (pokkum-init, pokkum-static) shares the root go.mod but needs its own explicit go build/go test, and tests/integration's golden-manifest and runtime-smoke suites also sit outside make verify's canonical five steps."
    problem: |
      `make verify`'s five steps run `go test ./internal/...` plus a `cmd/pokkum` build only.
      `supervisor/...` is a separate build target entirely, and a diff that only touches
      `supervisor/` can pass every one of the five steps while `go build ./supervisor/...` or
      `go test ./supervisor/...` are actually broken. Separately, `tests/integration/golden_test.go`
      (OCI manifest/config/index goldens) and the runtime-smoke suites are also outside `make
      verify`'s scope and must be run explicitly for any change touching layer compression, tar
      construction, OCI assembly, or a produced image's boot behavior. Both facts are currently
      documented only as a reminder in `CLAUDE.md` and Serena's `mem:state`, not enforced by
      tooling — nothing fails a CI run or a `make verify` invocation that skips them.
    options:
      - title: "Fold supervisor/ into a sixth make verify step"
        description: "Add go build ./supervisor/... && go test ./supervisor/... as a mandatory sixth step, always run rather than conditionally on the diff touching supervisor/."
        tradeoffs: "Simple and impossible to forget, but adds supervisor's build/test time to every make verify run even when nothing under supervisor/ changed."
      - title: "Path-based CI trigger"
        description: "Add a CI job that runs only when the diff touches supervisor/ or tests/integration/, keeping the fast local make verify loop unchanged."
        tradeoffs: "Keeps the common case fast, but a local make verify still can't be trusted alone for a supervisor-touching change — a contributor without CI access could still miss it."
    recommendation: "Fold supervisor/ into make verify unconditionally, since its build/test time is small relative to the rest of the suite and 'always run' is the only version of this that can't be silently skipped; keep tests/integration's heavier suites as an explicit, documented extra step given their real Docker/network cost."

  - id: cli-workflow-tests
    title: "Multi-command CLI workflow tests"
    stage: v1.1
    status: shipped
    kind: infra
    tier: foundation
    summary: "Run the real binary through command sequences in one project directory, covering whether commands work together rather than only in isolation."
    problem: |
      Every test in `tests/integration` drove Pokkum through its Go API. That is the right level
      for most things, but it left a whole class uncovered: whether the commands work *with each
      other*. `pokkum init` shipped writing an `sbom.attach` value `pokkum build` refused, so the
      first two commands a new user runs were incompatible, and nothing caught it — no test ever
      ran one command's output into the next. It was found by a maintainer typing the two
      commands, after six waves of hardening had passed over the codebase.

      Unit tests cannot close that gap by construction: they assert the pieces, and the failure
      lived in the seam between two individually-correct pieces.
    decision: |
      Shipped 2026-08-19. `tests/integration/cli_workflow_test.go` builds the CLI once per run
      and executes declared step sequences against one project directory, so each command sees
      what the previous one left behind. Five workflows: init→validate→build (the reported bug),
      init idempotence over a hand-edited config, a deliberately corrupted config failing with
      the offending field named, `--output=json` parsing at every step, and the local-profile
      round trip (profiles merge through different code than the base config).

      The load-bearing assertion is the *absence* list, not the exit code. This project has no
      registry configured, so `build` fails either way — what distinguishes "build rejected
      init's own output" from "build wants a repo" is which error appears, so each workflow
      names the config-rejection shapes that must not be present.

      Fast by default: nothing needs Docker, Bun, a registry or the network, because a build that
      stops at request validation still exercises the whole config load-and-validate path, which
      is where the bug lived. The five workflows run in about three seconds. Steps needing a real
      toolchain are gated on -short.
    impl:
      - tests/integration/cli_workflow_test.go
    related: ["init-generates-invalid-config"]


  - id: performance-benchmark-harness
    title: "A benchmark harness, and the optimisation pass it made possible"
    stage: v1.2
    status: shipped
    kind: dx
    tier: foundation
    summary: "Nothing in this repo measured speed, so every performance claim was an argument from reading the code; adding benchmarks over realistic inputs turned that into numbers, and the numbers then found two bugs that correctness tests structurally could not."
    decision: |
      Shipped 2026-09-05. Before this there were zero Benchmark functions in the module, the
      pipeline recorded only a single whole-build duration, and benchmarks/three-way measured
      image size, package count and CVEs but had no wall-clock row. `make bench` now covers the
      build hot paths and both PID-1 binaries, with GOTOOLCHAIN pinned the way the supervisor
      targets pin it, since numbers taken under whatever Go happens to be installed are not
      comparable to CI's.

      Every benchmark carries a fixture floor that fails the run if it would otherwise measure
      the wrong path — that pruning actually pruned, that the cache entry was present on the hit
      path, that the scanner scanned rather than skipped. Several of those floors were shown to
      go red before being kept.

      The optimisation pass that followed is measured against that baseline. Largest results:
      the layer-cache hit path went from 24.3ms to 33us and the Bun custom-file layer hit from
      157ms to 37us (both were fully decompressing and re-hashing a blob on every hit); secret
      scanning went from 4.5 to 92.6 MB/s over a directory and 4.2 to 180.7 MB/s over a large
      bundle; IsJunk went from 4535ns to 108ns per path; the ignore matcher from 1611ns to 242ns
      at zero allocations; precompression memory from 16.3GB to 1.8GB; per-container-start
      attestation from 648ms to 387ms with 89% less memory; and static serving improved on five
      of six request shapes, with the 404 path 14x faster. Registry auth sessions for a signed
      two-platform push dropped from roughly 26 to 8, and directory-tree layers are now built
      once per build rather than once per platform, so multi-platform layer work is flat in
      platform count instead of linear.
    limitations:
      - "Two bugs were found only because something finally counted: the remote build cache could never hit on any project since it shipped (pokkum.lock was hashed as source while the build stamps time.Now() into it beforehand), and a credential cache stored only its successes so every registry without a stored credential re-spawned the helper subprocess forever. Both are invisible to correctness tests — a cache that never hits behaves exactly like one that correctly misses. See Lessons.md 2026-09-05 and mem:self_review_checklist rows 62 and 63."
      - "The large-tree layer benchmark is too noisy on a loaded machine to support a timing claim (a 1.9x spread was observed across runs of identical code); allocations are the reliable signal there, and the multi-platform variant is what actually demonstrates the per-build memo."
      - "One request shape regressed deliberately: a conditional 304 went 23.8us to 27.4us, because the ETag is now taken from the open handle that is actually served, which is what closes the TOCTOU window the old resolve-then-stat-then-open path had. Documented at the benchmark case."
      - "A typed bun.lock decode was built four ways, measured slower than the map[string]any code it would replace every time, and reverted; the measurements live in a doc comment on ParseBunLock so the next attempt does not repeat them."
    impl:
      - Makefile
      - internal/adapters/packager/bench_test.go
      - internal/adapters/secretguard/bench_test.go
      - supervisor/cmd/pokkum-init/attest_bench_test.go
      - supervisor/cmd/pokkum-static/server_bench_test.go
    related: ["verify-suite-scope-gaps"]

  - id: vendor-cancellation-test-flake
    title: "TestPrepare_CancelledContextLeaksNeitherInstallNorGoroutine is timing-dependent"
    stage: v1.2
    status: shipped
    kind: infra
    tier: polish
    summary: "The vendor-cancellation guard races a real `bun install` against context cancellation, and fails on a slow or loaded runner when the install wins."
    problem: |
      `internal/adapters/bunexec/vendor_concurrency_test.go` asserts that cancelling `Prepare`
      tears down the concurrent vendor install. It observed the opposite on the macOS CI job
      during the v1.2.0 release check — "the vendor install outlived the cancelled Prepare and
      ran to completion" — and passed on a re-run of the same commit with no change.

      It is not a regression: the test predates the branch that surfaced it, and that branch
      touched no cancellation, goroutine or vendor code in `bunexec`. It passed three of three
      local runs on the same OS.

      The mechanism is a race the test creates rather than one it observes: it starts a real
      install and cancels shortly after, so on a runner where the install finishes first the
      assertion fires even though cancellation works correctly. A guard that reports a failure
      when the code is right is worse than no guard — it trains readers to re-run rather than
      to look, which is exactly what happened here.
    decision: |
      The race was between two independent clocks: a fixed `sleep 3` fake install, and however
      long cancellation took to propagate (context → the vendor job's goroutine → `cmd.Cancel` →
      SIGKILL). On a loaded runner the second could exceed the first with teardown working
      perfectly.

      The fake install now blocks forever, so it has no natural end and the only way it can stop
      is by being killed — there is no clock left to lose against. The assertion moved from the
      completion proxy ("a marker file never appeared") to the signal that actually matters: the
      OS process is gone, probed with signal 0. The goroutine-leak half is untouched; both halves
      the test name promises are still asserted.

      No production seam was needed — the existing `cmd.Cancel`/process-group-kill machinery
      already provided everything.

      One consequence of a fake install that never exits on its own: if the assertion fails, that
      process outlives the run and sits in a sleep loop indefinitely. Proving the guard could
      fail left exactly one such orphan behind, found with `ps` and killed by hand. So the kill
      is unconditional `t.Cleanup` rather than part of the assertion — a red test must not also
      litter the machine.

      Verified 20/20 consecutive runs, and 10/10 under `-race`.
    impl:
      - internal/adapters/bunexec/vendor_concurrency_test.go
    related: ["race-detector-and-coverage-floor"]
